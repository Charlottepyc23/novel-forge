## [1.7.3] - 2026-08-25

### 🛠 整体体检与修复

- 修复章节列表潜在崩溃：`NovelPanel.statusBadge` 遇未知 `chapter.status` 返回 `undefined`，补 `default` 错误徽章
- 修复 `/config` 丢字段：`patchConfig` 现在会拷贝 `imageModels`（多生图模型库可持久化）
- 修复运行中换书写错目录：`applyDraft` 改用生产单绑定目录（`bookDir`）
- `open-folder` 改用 `spawn('explorer',[dir])`，规避 Windows cmd 元字符注入
- 构建产物对齐：重跑 `pnpm build`，`lib` 与 `src` 同快照（补 `story-beat-language.d.ts`、`protocol` 用 `EmotionId[]/StoryFunctionId`、清除孤儿 `RoleVisualPanel.d.ts`）
- 发布：`files` 补 `skills/`、去掉从不存在的 `*.d.ts.map`
- 文档：README 更新为“可配置内置生图模型”；CHANGELOG 版本排序修正、移除游离标题
- 测试：新增 smoke 测试（`story-beat-language` / `shot-language`），`pnpm test` 通过
- 可访问性：ShelfView 开书/导入卡、SceneLibrary 场景卡、CreateBookView 大纲拖拽区补 `role/tabIndex/aria-label/键盘`；ImportModal 支持 Esc 关闭（`role=dialog`）
- 健壮性：RunPanel 统计字段改可选链（`run.stats?.x ?? 0`）

---

## [1.7.2] - 2026-08-22

### ✨ 新增「粘土拟态」主题（Claymorphism）

- 主题下拉新增「粘土拟态 · 柔和黏土」（value=clay，localStorage 记忆，随主题切换）
- 视觉：柔软圆润、奶油底色、大圆角、同色系软双阴影（落地 + 内阴影）、顶部奶油高光，实心无玻璃模糊
- 深浅两套配色（默认浅奶油 / 深棕暖色），控件圆形凹陷内阴影
- 完全复用现有令牌体系（--nf-card-* / --nf-ctrl-* / --nf-glass-*），仅覆盖颜色/阴影/形态，不引入新尺寸

---
## [1.7.1] - 2026-08-22

### ✨ 样式尺寸全站统一（主题无关）

- 新增统一尺寸令牌体系（--nf-fs-* / --nf-space-* / --nf-radius-* / --nf-ctrl-*），作为单一尺寸来源
- 字号统一：次要文本 12px、主文本 14px，标题档 16/18/20/22/24/34/42；设置页放大字号移除，与全站一致
- 间距 / 圆角 / 控件高度全部令牌化（panel.module.css 500 处 + TSX 内联 300+ 处）
- 页面主容器统一：工作台 / 书架 / 开书向导 / 设置页共用 --nf-container-pad / --nf-card-pad（以设置页 20px 22px 为基准）
- 界面密度三档（舒适/紧凑/宽松）改为只重定义尺寸令牌，覆盖全站主容器；主题只改颜色质感，永不触碰尺寸
- 新增 scripts/check-theme-sizes.mjs（主题块零尺寸声明校验，pnpm check:styles）与 scripts/check-fallback-tiers.mjs
- 修复历史遗留：.settingsCard + .settingsCard 块缺闭合括号导致 .switch 规则被嵌套吞并

---
## [1.7.0] - 2026-08-22

### ✨ 设置页全面重构

- 设置页内子导航（模型与推理 / 写作与审稿 / 生图模型 / 路径与文件 / 外观与主题），大卡片纵向疏朗布局
- 分页容器满高自适应，随浏览器窗口伸缩
- 字体 / 标题加大；原生下拉配色修复（防「白底白字」）

### ✨ 外观与主题

- 显示模式：跟随系统 / 强制浅色 / 强制深色（只作用于小说工坊面板）
- 主题风格下拉：液态玻璃 · 清新绿 / 经典毛玻璃 · 沉静蓝 / 新拟物 · 柔和浅色
- 界面密度：舒适 / 紧凑 / 宽松（卡片、导航、内容间距统一缩放）
- 「恢复默认主题」一键还原

### ✨ 生图模型库

- 支持多套 OpenAI 兼容生图接口：名称 / 接口地址 / API Key / 模型 id，可增删、启用一条生效
- 总开关滑块（启用生图功能）独立于模型选择
- 每个模型条目「测试」按钮：连通 + 延迟（ms）+ 模型列表校验
- 角色详情「AI 生图」可选模型（缺省用启用条目）
- 不再写死豆包：地址 / Key / 模型全部可配；旧配置自动迁移为「豆包（旧配置）」，删除后不再复活

### ✨ 图标与外观

- 引入 lucide-react：左导航 / 页签 / 设置页图标全部 SVG 化（emoji → 线性图标，随主题换色）
- 按钮质感升级：悬停上浮、内顶高光、主按钮渐变 + 外发光、圆角收敛
- 书架页：顶部玻璃卡（AI小说工坊 · 书架）、统计卡=可点击筛选、封面 hover 动效、按钮吸底对齐
- 「工作进度」更名「AI进度」

### 🛠 其他

- 桌面 DSH启动.bat：端口检测、隐藏进程启动、自动打开浏览器
- 编辑器字号说明明确化（正文编辑区 / 工作区显示字号）

---
## [1.6.0] - 2026-08-22

### ✨ 场景库完整版搬入漫剧工作台

- 漫剧工作台「场景」区升级为完整场景库：提炼 / 采纳 / 修改后采纳 / 编辑 / 删除 / 图集上传 / 详情浮窗（原左导航「场景库」整体迁入，入口删除）
- 场景详情浮窗修复：createPortal 挂载（不再顶到顶部 / 无法关闭）、Esc / 遮罩关闭、全局中英切换 + 复制全部
- 场景提示词支持风格强制注入（LLM 漏嵌时自动补当前方案风格词块）
- 本书视觉规则只在主页面显示一次，浮窗内不再重复

### ✨ 角色形象

- 详情浮窗 createPortal 修复（漫剧工作台 / 角色形象页共用）
- 提示词包改版：全局中英切换 + 复制全部 + 立绘 / 四视图 / 表情分栏卡片
- 浮窗顶部「提示词风格」徽章；与当前方案不一致时黄条提示重新生成
- 浮窗内移除重复的「本书视觉规则」块

### ✨ 风格与匹配兜底

- 风格词块强制注入：角色锚点 / 四类精修包、场景、分镜视频提示词生成后自动补齐（zh 段首 / en 末尾）
- 形象锚点检索升级：最近 60 章 → 全书回扫 → 角色名 / 身份智能拆分（如「周野律师」→「周野的律师」「律师」「周野」）

### ✨ 分镜工作台

- 重新生成级联清理：新骨架自动清旧分镜表 + 旧提示词；新分镜表自动清旧提示词（旧风格缓存不再残留）
- ① 重新生成后自动进入 ② 并重算分镜表；② ③ 提供独立「重新生成」按钮
- 视频提示词按当前方案风格强制注入

### ✨ 导航与体验

- 左导航精简：删除「角色形象」「场景库」「风格库」入口（功能并入漫剧工作台），「漫剧」更名「漫剧工作台」
- 「工作进度」接入漫剧工作台全部操作（方案 / 角色 / 场景 / 分镜的开始·成功·失败上报）
- 左导航字体 / 间距整体调大

---
## [1.5.0] - 2026-08-21

### ✨ 新增：漫剧工作台（辅助型视觉化工作流）

- **🎬 漫剧工作台**：同一本书可建多套视觉方案（基底风格 + 可选滤镜，如《保质期》3D 皮克斯版 / 底层赛博朋克版），方案切换驱动整条视觉链路
- **🎨 风格库**：21 个内置风格模板（3D / 游戏 3D / 2D 二次元 / 影视实验质感 / 国风工艺 五大分类），含风格效果图（随包内置，新用户安装即见），关键词一键复制
- **分镜三步流**：① 编剧级剧情骨架（弧线 + 节拍链，故事层多方案共享）→ ② 导演级分镜表（景别/运镜/时长/画面/台词/音效/光效/状态连续，画面措辞随方案风格）→ ③ 即梦视频提示词（风格词块 + 画面 + 运镜 + 光效，逐镜头复制即用）
- **角色图按方案重出**：角色形象锚点/四类精修提示词自动注入激活方案的风格词块（替换原固定 3D 动漫词）
- 骨架/分镜表/提示词按章持久化到项目，切章/刷新不丢

### ✨ 其他（延续未发布累积）

- 导入点选文件 + 拆章预览；拆章支持中文数字/序章/尾声/Chapter N/GBK 编码
- 总纲页「反推大纲」；角色提炼修复（全书采样 + 主角反派必含 + 自动重试）
- 设置页分组改版；分析类任务推理档位（默认 low）

---
## [1.4.1] - 2026-08-21

### ✨ 新增

- **txt/md 导入体验升级**：点击选择文件即可导入（不再只支持粘贴服务器路径），选文件后先做拆章预览（识别章数/跳过清单/书名），确认后再落盘导入
- **拆章识别增强**：支持中文数字章节（一、二、三…）、序章/楔子/尾声/番外等特殊章节、英文 Chapter N；GBK/GB18030/UTF-16 编码自动适配；目录与正文重复标题自动剥离
- **🔄 反推大纲**：总纲页一键从已写章节正文反向生成全书总纲（分卷 + 章节要点 + 主线/人物弧线/伏笔清单），导入的书也能补齐创作骨架
- **分析类任务推理档位**：设置页新增「分析类任务思考强度」（默认 low），角色/场景/道藏提炼、拆书、反推大纲等一次性分析任务独立生效，不再被写作档位（off）拖累质量

### 🔧 修复

- **角色提炼漏主角/反派**：正文摘录改为均匀采样覆盖全书（开头/中间/结尾），精简输入（摘录与编年录限量），系统提示强制主角/反派必含，空结果或缺主角时自动重试一次
- 总纲页重复的「更新大纲」按钮合并为标题栏单一入口

### 💄 界面

- 设置页按功能分组为五张卡片：📁 路径与输出 / 🤖 模型与推理 / 🎨 生图（豆包）/ ✍️ 写作与审稿参数 / 💾 保存与导出
- 设置卡片字段对齐（标签单行、输入框顶部对齐），「审稿通过分数」下方常驻判定说明

---
## [1.4.0] - 2026-08-21

### ✨ 新增

- **书架导入小说（两种模式）**：
  - **A · 导入已有项目目录**：粘贴含 `novel-project.json` 的目录即可登记到书架并激活，工作台大纲/设定/章节/伏笔/资产全部按原项目加载；已在书架中的目录重复导入会直接切换激活，不产生重复条目
  - **B · 导入 txt/md 全本**：粘贴全本文本文件路径，服务器按「第X章/第X回/第X节/第X卷」（中文数字也支持）自动拆章落盘为章节 Markdown，新建项目并登记书架；章节以「written（待审稿）」状态进入，可逐章审稿/补设定；正文过短的章节自动跳过并列出
- 书架首页新增「📥 导入已有小说」入口（空书架与书卡网格均有），导入后自动刷新并切到该书

---

## [1.3.3] - 2026-08-21

### 🔒 隐私与默认值清理

- 移除代码中的私人路径与私人书目（默认输出目录改为 `~/.dsh/novels`，大纲路径默认空、在设置页配置）
- 移除含私人小说内容的示例/诊断脚本（outline-sample.txt 等），npm 包 files 仅保留必需 patch 脚本
- 修复公开仓库/他人在本机安装时的默认路径指向问题

---

## [1.3.2] - 2026-08-21

### 🔧 修复

- **跨书数据污染防线**：角色/场景不存在时报错携带当前激活书名（"当前激活《XXX》，角色 X 不存在——若期望的书不对，请先在书架切换该书并刷新"），多书切换时一旦错位立即暴露，不再静默写错项目
- 残留文案清理：角色形象页/设置页移除"漫画功能已移除"等历史注释，改为使用指引

### ✨ 优化

- 提示词模板升级（对齐主流生图工具输出结构）：立绘/四视图/表情/细节四类提示词统一为「构图定位 → 风格（3D动漫超精细建模）→ 纯白纯色背景 → 身份 → 五官 → 气质 → 服装分件 → 标志物 → 收尾约束」字段流
- 移除角色详情「重要/配角/路人」下拉（漫画功能残留，数据字段保留兼容）

---

## [1.3.1] - 2026-08-21

### 🔧 修复

- **npm 安装失败**：files 白名单补入 scripts/ 目录，修复 postinstall（patch-schemastery.mjs）在安装时找不到脚本导致安装失败的问题；1.3.0 的 npm 包不可安装，请升级 1.3.1

---

## [1.3.0] - 2026-08-21

### ✨ 新增

- **场景库（🏞️）**：从全书提炼「镜头场景」级视觉锚点——五幕结构（后场/卖场/禁忌区/外界/抉择）、时间光态、关键情节镜头、人物在场状态、环境构成/色调/氛围、中英文生图提示词、场景图集（全景/局部/氛围）
- **视觉世界观规则**：从道藏提炼 3-6 条生图纠偏规则（如"货架上的一切商品都是活人，禁止画成罐头/饮料/日用品"），自动注入所有角色/场景提示词，解决反常识设定（人=商品）被生图模型画跑偏的问题
- **角色提示词分发中心**：角色详情悬浮页提供 立绘 / 四视图 / 表情×N / 细节 四类生图提示词，中文默认显示 + 英文标签折叠切换；支持 LLM 精修版（一次提炼直接产出四类，字段化 + 出图约束对齐行业模板）
- **角色表情清单**：提炼时自动补全角色所需 6-12 个情绪表情（如 疲惫/麻木/压抑悲伤），表情提示词保持同一脸部锚点
- **角色图集（gallery）**：立绘/四视图/表情/场景/细节 分类上传、展示、删除，图片锚点与文字锚点并存
- 审稿新逻辑：无 high 问题且评分 ≥60 直接通过（不再 72 分挂起等待批后豁免）
- 续写规划修复：精简大纲全量注入（不再截断丢失分卷收官规划，如第五卷双结局）；章节列表只列最近 80 章；接近收尾区间强制规划大结局（禁止悬念收尾）

### 🗑 移除

- 漫剧分镜 / 漫剧分集计划 / 漫画脚本 功能（代码、API、UI、comfyui 工作流文件全部移除）
- 角色参考图风格下拉选择器（风格预设保留在服务端，前端不再暴露）

### 🎨 界面

- 角色形象页改版：卡片列表 + 悬浮详情面板（对齐角色设定表排版：立绘+四视图并排、表情宫格、基本信息、关键词标签、提示词包）
- 角色/场景详情：图集默认折叠，提示词为主展示

---

## [1.2.0] - 2026-08-19

### 新增

- 工作台（创作工作流页）升级为 demo 风格：统计卡（今日待写/写作中/已通过/待审稿）、创作时间线（6 阶段分段条，点击跳转对应页面）
- 侧边栏改版：全高玻璃侧栏、标题区（小说工坊 + 当前书名）、底部当前书切换卡、收起/关闭操作行（顶部标题栏取消）
- 导航重组（对标 AI-Novel-Writing-Assistant）：
  - 创作：工作流 / 大纲 / 简介·封面 / 章节 / 长线管理 / 本书设定
  - 工具：AI 助手 / 工作进度 / 拆书分析 / 漫剧工坊 / 生产单
  - 资产：创作资产（题材基底/推进模式/笔法帖/文戒/心法 五合一页签）
- 合并页：长线管理（剧情线 + 伏笔）、编年·复盘（编年录 + 复盘记录）、本书设定（设定库/大世界/角色库/编年·复盘），子页签 localStorage 记忆
- 页面级切换标签全局统一：大标题 + 等宽页签行样式
- 创作资产页：状态总览四格升级玻璃卡（含 hover 反馈），补充页内 5 子页签
- 全书质检实时状态（/status 暴露进度，工作台显示批次数进度条）
- 设置：模型思考强度 reasoningEffort（off/low/high/max）配置

### 修复

- 角色库/复盘/剧情线卡片容器溢出问题（flex: 1 + minHeight: 0 导致卡片超出容器）
- 拆书分析下拉框选项显示不全（放宽宽度 + 精简文案）
- 工作台双绿色进度条重复显示

### 变更

- 「卷首语」更名为「简介 / 封面」；「生产单」移入工具组
- 数据库组重组：单书资料（设定库/大世界/角色库/编年·复盘）收进「本书设定」，资产组保留跨书语义（当前仍按书存储，全局化待后续版本）

## [1.1.9] - 2026-08-19

### 修复

- 修复项目跨盘/跨目录移动后残留的旧路径问题
- 修复 dsh web 启动时 novel-forge 因 `schemastery` 缺少 `exports` 导致的加载失败
- 修复 `@linxin666/dsh-client-ui-web-ui-settings` 在 rc.7 下 keyed slot 报错

### 变更

- 插件与小说目录统一到新结构（插件位于插件目录，小说输出目录可在设置页配置，默认 `~/.dsh/novels`）
- 新增 `scripts/patch-schemastery.mjs`，在 `pnpm install` 后自动补回 `schemastery` 的 `exports`，避免再次启动失败

### 其他

- 保持本地链接方式安装，无需重新发布即可使用
